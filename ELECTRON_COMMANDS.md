<div dir="rtl" align="right">

# פקודות Electron — מדריך הרצה ובנייה (למען)

מדריך מלא לכל פקודות ה‑npm של אפליקציית הדסקטופ: מה כל אחת עושה, **המחשה מהחיים**, ומתי להשתמש בה.

---

## 📍 מאיפה מריצים את הפקודות?

תמיד מ**התיקייה הכללית (השורש)** של הפרויקט — זו שמכילה את `package.json`:

```
C:\Users\jj121\Documents\lemahan\deepfocus-zone
```

npm מחפש את `package.json` בתיקייה הנוכחית כדי לדעת מהם הסקריפטים — לכן **חייבים** להיות בשורש. אם פתחת טרמינל במקום אחר, קודם:

```bash
cd C:\Users\jj121\Documents\lemahan\deepfocus-zone
```

> ⚠️ **לא** מריצים מתוך `dist/` או `release/` — אלו תיקיות **פלט** בלבד (המחשב כותב אליהן, אתה לא עובד מתוכן). את קובץ ה‑`.exe` שבתוך `release/` פשוט לוחצים עליו פעמיים כדי להתקין — לא דרך npm.

---

## 🧠 מושגי יסוד (חשוב להבין פעם אחת)

כדי שהפקודות יהיו הגיוניות, נדמיין ש**הפרויקט שלך הוא מטבח של מסעדה** 🍽️:

| במטבח | בפרויקט | הסבר |
|--------|----------|------|
| מתכונים + חומרי גלם | **קוד המקור** (`src/`) | מה שאתה כותב ועורך. בני אדם קוראים את זה, המחשב עדיין לא יכול "להגיש" את זה ישירות. |
| הכנת המנה המוכנה ואריזתה במקרר | **bundle** (תיקיית `dist/`) | לקחת את כל המתכונים והחומרים ולבשל מהם מנה אחת מוכנה, ארוזה ומצומצמת — מוכנה לחימום מיידי. |
| מטבח פתוח שבו טועמים תוך כדי בישול | **שרת פיתוח + Hot Reload** | כל שינוי שאתה עושה נטעם מיד, בלי לבשל הכל מחדש. |
| קופסת משלוח סגורה עם מדבקה | **installer** (`release/*.exe`) | המנה ארוזה בקופסה שאפשר לתת ללקוח שיתקין אצלו. |

**אז מה זה "bundle"?**
ה‑bundle הוא **התוצר המבושל והארוז של הקוד**: Vite לוקח את כל קובצי ה‑`.tsx`, ה‑CSS, התמונות — ממזג, מכווץ וממיר אותם למספר קבצים סטטיים בתיקיית `dist/` שהדפדפן/Electron יודע להריץ ישירות. זה עדיין **לא** קובץ התקנה — זו רק "המנה המוכנה במקרר".

---

## 1. הרצה כמפתח — פיתוח חי (Hot Reload)

```bash
npm run electron:dev
```

**מה עושה:** מריץ במקביל את שרת הפיתוח של Vite (פורט **5000**) ופותח חלון Electron שטוען ממנו. כל שינוי בקוד מתעדכן מיד בחלון בלי בנייה מחדש.

**🍳 המחשה:** זה **המטבח הפתוח**. אתה מבשל וטועם תוך כדי — משנים תו אחד בקוד, והחלון מתעדכן לבד תוך שנייה. לא צריך "לבשל הכל מחדש" בכל שינוי.

**מתי להשתמש:** בפיתוח היומיומי, בכל פעם שאתה עובד על התכונות ורוצה לראות תוצאות בזמן אמת. **95% מהזמן תשתמש בזה.**

**מאחורי הקלעים:** `concurrently` מריץ `vite` + (‏`wait-on http://localhost:5000` ← ‏`electron .` עם `ELECTRON_DEV=1`‏).

---

## 2. הרצה זמנית / פריוויו — גרסת פרודקשן מקומית

```bash
npm run electron:preview
```

**מה עושה:** קודם בונה את ה‑bundle (‏`electron:build`‏), ואז פותח Electron שטוען את הקבצים **הבנויים** מ‑`dist/` — בדיוק כמו אצל משתמש אמיתי. בלי שרת פיתוח, בלי Hot Reload.

**🍽️ המחשה:** עכשיו אתה **מגיש לעצמך את המנה המוכנה על השולחן ואוכל אותה כמו לקוח**. לא טועם תוך כדי בישול — אלא בודק את התוצאה הסופית בדיוק כפי שהיא תגיע למשתמש.

**מתי להשתמש:** לפני שחרור, כדי לוודא שהגרסה ה"אמיתית" (הבנויה) עובדת חלק — כי לפעמים משהו עובד ב‑dev אבל מתנהג אחרת בבנייה האמיתית. **זו בדיוק הבדיקה הנכונה לוודא שהמצב אופליין עובד כמו שצריך.**

**שים לב:** אחרי כל שינוי בקוד צריך להריץ שוב (אין עדכון אוטומטי — המנה כבר "בושלה").

---

## 3. בניית ה‑bundle בלבד (הבילד)

```bash
npm run electron:build
```

**מה עושה:** ‏`cross-env BUILD_TARGET=electron vite build`‏ — בונה את קבצי ה‑web לתיקיית `dist/` עם נתיבים **יחסיים** (‏`base: "./"`‏), כדי שיעבדו מתוך קובץ מקומי ב‑Electron. **לא** פותח חלון ו**לא** יוצר installer.

**📦 המחשה:** זה שלב ה**"בישול והכנסה למקרר" בלבד** — הכנת המנה המוכנה, בלי להגיש אותה (אין חלון) ובלי לארוז אותה למשלוח (אין `.exe`). רק מוודא שהמנה יצאה תקינה.

**למה כתוב "בלבד"?** כי הפקודה עושה רק את שלב הבנייה ונעצרת. היא **לא** מריצה את האפליקציה ו**לא** אורזת התקנה — בשביל זה יש את הפקודות 2 ו‑4.

**מה הרווח להריץ אותה לבד?** בכנות — לבד כמעט אף פעם לא צריך אותה. הערך שלה:

- **בדיקת "בונה בלי שגיאות?"** — מריצה רק את שלב הבנייה, מהר, בלי לפתוח חלון ובלי לארוז installer. אם יש שגיאת קוד שמפילה את הבנייה — תדע מיד, בלי לחכות לשלבים הכבדים.
- **אבן בניין** — `electron:preview` ו‑`electron:dist` שניהם מריצים אותה בתוכם. אם `electron:build` נכשל, אין טעם להמשיך הלאה.
- **מרענן את `dist/`** בלי כל השאר — שימושי אם כלי או סקריפט אחר צריך את הקבצים הבנויים, או באוטומציה (CI).

**מתי להשתמש:** בדרך כלל **לא צריך להריץ אותה לבד**. תריץ אותה רק כשתרצה **לוודא מהר שהקוד מתקמפל ונבנה בלי שגיאות**, בלי לפתוח כלום.

---

## 4. בילד אחרון — קובץ התקנה לפרודקשן / הפצה

```bash
npm run electron:dist
```

**מה עושה:** מריץ `electron:build` ואז `electron-builder` שאורז את הכל ל‑**installer**.

בווינדוס נוצר מתקין NSIS בתיקיית `release/`:

- `lemaan-setup-<גרסה>.exe` — מתקין רגיל (עם קיצור דרך בשולחן העבודה ובתפריט התחל).
- `lemaan-portable-<גרסה>.exe` — גרסה ניידת שרצה בלי התקנה.

**🎁 המחשה:** זה שלב ה**אריזה למשלוח** — לוקחים את המנה המוכנה, שמים בקופסה סגורה עם מדבקה, ומכינים אותה למסירה ללקוחות. זה הקובץ שאתה שולח לאנשים כדי שיתקינו את האפליקציה במחשב שלהם.

**מתי להשתמש:** כשמסיימים גרסה ורוצים **להפיץ אותה למשתמשים**. **זה ה"בילד האחרון" האמיתי.**

---

## פקודות עזר (web / איכות קוד)

```bash
npm run dev
```
שרת Vite בלבד **בדפדפן** (פורט 5000) — בלי Electron. שימושי לבדיקה מהירה בדפדפן.

```bash
npm run build
```
בניית web רגילה (לא Electron, נתיבים מוחלטים). לפריסה לאתר אינטרנט.

```bash
npm run lint
```
בדיקת ESLint על כל הקוד — מוצא שגיאות סגנון ובעיות נפוצות.

```bash
npm run test
```
הרצת כל הבדיקות (vitest) פעם אחת. להרצה רציפה: `npm run test:watch`.

---

## 🎯 מתי צריך מה? — לפי מטרה

| מה אני רוצה לעשות עכשיו | הפקודה |
|--------------------------|---------|
| לפתח ולראות שינויים תוך כדי, בזמן אמת | `npm run electron:dev` |
| לבדוק איך האפליקציה נראית ומתנהגת **למשתמש** לפני שחרור | `npm run electron:preview` |
| רק לוודא שהקוד נבנה בלי שגיאות (בלי לפתוח חלון) | `npm run electron:build` |
| ליצור קובץ `.exe` להתקנה ולהפצה למשתמשים | `npm run electron:dist` |

---

## 📊 טבלת סיכום מהיר

| מטרה | פקודה | פותח חלון? | Hot Reload? | יוצר קבצים |
|------|--------|:----------:|:-----------:|-------------|
| פיתוח חי (מפתח) | `npm run electron:dev` | ✅ | ✅ | — |
| הרצה זמנית / פריוויו פרודקשן | `npm run electron:preview` | ✅ | ❌ | `dist/` |
| בניית bundle בלבד | `npm run electron:build` | ❌ | ❌ | `dist/` |
| בילד אחרון / installer | `npm run electron:dist` | ❌ | ❌ | `dist/` + `release/*.exe` |

---

## 📁 מיקומי פלט

- **`dist/`** — ה‑bundle הבנוי ("המנה המוכנה"). מה ש‑Electron טוען בפרודקשן.
- **`release/`** — קובצי ההתקנה ("קופסת המשלוח"), כלומר ה‑`.exe`.

---

## ⚡ ההבדל המרכזי: dev מול preview

- **`electron:dev`** = מטבח פתוח, שרת חי + Hot Reload ← מהיר, לפיתוח שוטף.
- **`electron:preview`** = המנה המוגשת כמו ללקוח, מהקבצים הבנויים ב‑`dist/` ← לבדיקת פרודקשן אמיתית.

> 💡 **טיפ:** אם משהו עובד ב‑`electron:dev` אבל שבור ב‑`electron:preview` — הבעיה קשורה לבנייה עצמה (נתיבים, טעינת קבצים אופליין וכד'), לא ללוגיקה. תמיד כדאי לבדוק ב‑preview לפני שחרור.

---

## 🔄 סדר עבודה מומלץ

1. **מפתחים** עם `npm run electron:dev` (רוב הזמן).
2. **לפני שחרור** בודקים עם `npm run electron:preview` (כולל בדיקת מצב אופליין).
3. **יוצרים מתקין סופי** עם `npm run electron:dist` ומפיצים את ה‑`.exe` מתיקיית `release/`.

</div>
