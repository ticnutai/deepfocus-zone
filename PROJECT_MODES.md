<div dir="rtl" align="right">

# מדריך המצבים במערכת — מי רואה מה, מה נשמר, ומה נמחק

מסמך זה מסביר מה קורה בפועל לכל סוג משתמש (רשום · אורח · אופליין) ובכל סביבה (אתר · Electron), עם המחשות והסברים.

---

## 🧠 מושג יסוד: שלוש שכבות אחסון

לפני הכול — כדאי להבין איפה הנתונים חיים. דמיין **שולחן עבודה, מגירה, וארון בענן**:

| השכבה | איפה | מהירות | שורד ריענון? | שורד מחיקת אפליקציה? |
|---|---|---|---|---|
| 🖥️ **זיכרון** (memState) | RAM | מיידי | ❌ | ❌ |
| 🗄️ **localStorage** | הדפדפן | מהיר מאוד | ✅ | ❌ |
| 📦 **IndexedDB** | הדפדפן | מהיר | ✅ | ❌ |
| ☁️ **Supabase** | ענן | תלוי רשת | ✅ | ✅ |

**למה צריך גם localStorage וגם IndexedDB?**
`localStorage` מוגבל ל‑5MB בערך. הספרייה המובנית לבדה היא ~22,700 שאלות — הרבה מעבר לזה. לכן:

- **IndexedDB** = הארכיון הגדול (כל השאלות).
- **localStorage** = פתק מהיר לדברים קטנים (ערכת נושא, סדר טאבים) שנכתב מיד.

> 💡 **הסבר לבאג שתוקן:** בעבר ההגדרות נשמרו יחד עם כל הנתונים. כשהחבילה חרגה מהמכסה — הכתיבה נכשלה **בשקט**, וההגדרות נעלמו איתה. היום ההגדרות נשמרות ברשומה קטנה ונפרדת שנכתבת **ראשונה**, כך שהיא תמיד מצליחה.

---

## 👥 שלושת סוגי המשתמשים

### 1️⃣ משתמש רשום (מחובר לענן)

**איך נכנס:** אימייל/שם משתמש + סיסמה, עם אינטרנט.

| נושא | מה קורה |
|---|---|
| נתונים | נשמרים מקומית **וגם** מסתנכרנים לענן |
| מכשירים | אותם נתונים בכל מכשיר |
| מחיקה | מוחק מה שיצר; מחיקה מסתנכרנת לענן (soft-delete) |
| הרשאות | לפי תפקיד (admin / משתמש רשום / …) |
| חסימות | לפי פרופיל החסימות המשויך לתפקיד |
| אם האינטרנט נופל | ממשיך לעבוד מקומית, והשינויים נדחפים כשהחיבור חוזר |

**🏦 המחשה:** כמו חשבון בנק. הכסף אצלך בארנק (מקומי), אבל הרישום האמיתי בבנק (ענן). מכל סניף תראה את אותה יתרה.

---

### 2️⃣ אורח (Guest)

**איך נכנס:** כפתור "כניסה למצב אופליין" — בלי שם משתמש ובלי סיסמה.

| נושא | מה קורה |
|---|---|
| נתונים | נשמרים במכשיר הזה בלבד |
| ענן | ❌ אין סנכרון |
| זהות | אין — לא ניתן "להתחבר לאותו אורח" ממכשיר אחר |
| חסימות | ✅ **חלות** — לפי החסימה הגלובלית |
| מתאים ל | התנסות מהירה, הדגמה |

**🏨 המחשה:** כמו חדר במלון. אפשר לישון ולהשתמש בהכול, אבל מה שתשאיר שם לא באמת שלך ולא יעבור איתך.

---

### 3️⃣ אופליין עם שם משתמש (חשבון מקומי)

**איך נכנס:** לשונית "הרשמה" ← שם משתמש + סיסמה, **גם בלי אינטרנט**.

| נושא | מה קורה |
|---|---|
| נתונים | ✅ נשמרים במלואם, לצמיתות, במכשיר |
| ריבוי חשבונות | ✅ כמה חשבונות במחשב אחד, לכל אחד סביבה מבודדת |
| החלפה | דרך מתג החשבונות בסרגל הצד |
| מחיקה | ✅ מותרת — **רק על תוכן שהמשתמש הוסיף** |
| חסימות | ✅ **לא חלות** — פרופיל חסימות נפרד ופתוח |
| הרשמה לענן | אוטומטית ברגע שיש אינטרנט |

**🏠 המחשה:** כמו דירה בבעלותך. הכול שלך, נשאר שלך, ואתה יכול לשנות — אבל **קירות הבניין (הספרייה המובנית) לא ניתנים להריסה**.

#### כללי שם משתמש וסיסמה באופליין

- **שם משתמש:** ✅ עברית מותרת, וגם אנגלית/מספרים/נקודה/קו־תחתון. מינימום 2 תווים, בלי רווחים.
- **סיסמה:** ✅ ללא מגבלות אורך או מורכבות.
- **אימייל:** ✅ אופציונלי לגמרי.

> ⚙️ **איך זה עובד מתחת למכסה:** לשרת נדרשת כתובת מייל חוקית, ו‑`יעקב@users.local` אינה חוקית. לכן שם עברי ממופה אוטומטית לכתובת ASCII יציבה — למשל `יעקב` ← `user.1cm56wa@users.local`. השם העברי נשמר לתצוגה; המיפוי דטרמיניסטי, כך שאותו שם תמיד מקבל אותה כתובת. שמות באנגלית נשארים כמו שהיו (`david123@users.local`) לשמירת תאימות לאחור.

---

## 🛡️ מחיקה מוגנת — מה מותר למחוק

זהו ההבדל המהותי בין תוכן שלך לתוכן של האפליקציה:

| סוג תוכן | ניתן למחוק? | למה |
|---|---|---|
| שאלה שיצרת | ✅ כן | התוכן שלך |
| ערכה/קטגוריה שיצרת | ✅ כן | התוכן שלך |
| שאלה מהספרייה המובנית | ❌ לא | הגיעה עם האפליקציה |
| קטגוריה מהספרייה המובנית | ❌ לא | מחיקתה מפילה גם את כל מה שתחתיה |

**📚 המחשה:** ספרייה עירונית. הספרים שהבאת מהבית — קח מתי שתרצה. הספרים של הספרייה — אפשר לקרוא, לא לקרוע.

**למה זה קריטי דווקא באופליין?** למשתמש רשום יש עותק בענן — מחיקה בטעות ניתנת לשחזור. במצב מקומי **אין גיבוי**: מחיקה היא סופית, והדרך היחידה חזרה היא התקנה מחדש.

**עיקרון הנדסי — כשלון פתוח (fail-open):** אם רשימת המזהים המוגנים לא נטענה, ההגנה **לא** חוסמת. עדיף להחמיץ הגנה מאשר לחסום בטעות משתמש מלמחוק את התוכן שלו.

---

## 🖥️ אתר מול Electron

| נושא | אתר (דפדפן) | Electron (דסקטופ) |
|---|---|---|
| טעינה | מהשרת | מקובץ מקומי (`file://`) |
| עבודה בלי אינטרנט | חלקית | ✅ מלאה |
| ספרייה מובנית | הורדה | ✅ ארוזה בפנים |
| התחברות Google | ✅ | ❌ (לא נתמך ב‑`file://`) |
| ריבוי חשבונות מקומיים | ✅ | ✅ |
| עדכונים | אוטומטי | התקנה מחדש |

> ⚠️ **מלכודת ידועה:** ב‑Electron הערך `navigator.onLine` מחזיר `true` בכל פעם שיש כרטיס רשת פעיל — גם בלי אינטרנט אמיתי. לכן הקוד **לא סומך עליו**: הוא מנסה לפנות לשרת עם timeout של 6 שניות, וכל כשלון רשת נחשב "אופליין".

> 🔒 **מופע יחיד:** Electron מרשה מופע אחד בלבד. אם האפליקציה המותקנת רצה, הרצת גרסת פיתוח **תיסגר מיד בשקט עם קוד יציאה 0** — בלי הודעת שגיאה. צריך לסגור את המותקנת קודם.

---

## 🚫 מערכת החסימות (Blocklist)

מנגנון שמאפשר להסתיר מקטעים וּווידג'טים לפי תפקיד.

**סדר ההכרעה:**

1. יש לתפקיד פרופיל חסימות משויך? → **הפרופיל קובע לבדו** (אין איחוד עם הגלובלי).
2. אין שיוך? → חלה **החסימה הגלובלית**.
3. התפקיד הוא `local-offline`? → **כלום לא חסום**.

**למה יש חריג לאופליין?** מכונה בלי אינטרנט לא יכולה לכתוב לשרת, ולכן פרופיל החסימות שלה לעולם לא ייווצר שם. בלי החריג היא הייתה נופלת לחסימה הגלובלית ומציגה מסך ריק — בלי שום דרך לתקן.

> 💡 **דוגמה אמיתית מהמערכת:** חסימה גלובלית של 28 מקטעים ו‑12 ווידג'טים גרמה לטאב "כללי" להיראות **ריק לגמרי**. הנתונים היו שם (28,072 קטגוריות נטענו) — פשוט הכול היה מוסתר.

**ניהול:** אזור ניהול ← ברירות מחדל ופריסה ← טאב חסימה. שם יש מונה חסימות, כפתור **"בטל את כל החסימות"**, וסימון **"· לא בשימוש"** לפרופילים שאינם משויכים לאף תפקיד.

---

## 🔄 מה נשמר אחרי ריענון

| פריט | נשמר? | איפה |
|---|---|---|
| ערכת נושא | ✅ | localStorage |
| סדר וניראות טאבים | ✅ | רשומת הגדרות |
| סדר סרגל הצד | ✅ | רשומת הגדרות |
| פריסת ווידג'טים | ✅ | רשומת הגדרות |
| התקדמות בלימוד | ✅ | IndexedDB |
| חשבון פעיל | ✅ | localStorage |

**איך זה עובד:** ההגדרות נכתבות **בכל שינוי, מיד**. הנתונים הגדולים נכתבים בהשהיה של ~2 שניות (כדי לא להאט את הממשק). בעלייה, ההגדרות האחרונות מוחלות **מעל** הנתונים — מאיזה מקור שלא יגיעו.

> 🐛 **הבאג שתוקן:** בעבר תמונת ה‑IndexedDB (שמפגרת ב‑2 שניות) הייתה דורסת את ההגדרות. אם שינית ערכת נושא וריעננת מיד — השינוי נעלם. גרוע מכך: סדר טאבים ישן היה נטען באמצע הרינדור, והלחיצות "קפצו" לטאב הלא נכון.

---

## ⚡ פקודות מהירות

```bash
npm run electron:dev      # פיתוח חי עם Hot Reload
npm run electron:preview  # בדיקה כמו משתמש אמיתי
npm run electron:dist     # יצירת קובץ התקנה
npm run test              # הרצת כל הבדיקות
```

מדריך מפורט: `ELECTRON_COMMANDS.md`

---

## 📌 טבלת ההשוואה המסכמת

| | רשום | אורח | אופליין+שם |
|---|:---:|:---:|:---:|
| נשמר במכשיר | ✅ | ✅ | ✅ |
| מסתנכרן לענן | ✅ | ❌ | אוטומטי כשיש רשת |
| עובר בין מכשירים | ✅ | ❌ | לאחר סנכרון |
| ריבוי חשבונות | — | ❌ | ✅ |
| מוחק תוכן שיצר | ✅ | ✅ | ✅ |
| מוחק ספרייה מובנית | ❌ | ❌ | ❌ |
| חסימות חלות | לפי תפקיד | ✅ | ❌ |
| שם משתמש בעברית | ❌ | — | ✅ |

</div>
