# תיקון המתנה ממושכת בכניסה — 18.09.2026

## ראיות לפני התיקון

- בצילום המשתמש הוצגה ההודעה „טוען את החשבון וההרשאות…” ללא התקדמות.
- בבדיקת WebView אמיתית בטלפון Samsung נמדדה בקשת `get_effective_content_access` באורך 2,202,528 מילישניות (כ־36.7 דקות). בזמן הדגימה החשבון כבר היה מזוהה כמנהל.
- אין בכך הוכחה שהבקשה הזאת לבדה גרמה לכל זמן החסימה; בזמן האיסוף מסך התקיעה כבר נעלם. אין לייחס את האירוע בוודאות לסיסמה או לשינוי תפקיד.
- בקוד נמצאה המתנה המסתמכת על AbortController בלבד: אם שכבת ההתחברות או התעבורה אינה מסיימת את ההבטחה, ביטול לבדו אינו משחרר את מצב הטעינה.

## השינויים

- שימוש בעזר משותף שמסיים את ההמתנה גם כאשר הפעולה מתעלמת מביטול. טעינת ההרשאות מוגבלת ל־8 שניות.
- שמירת כללי הזהות הקיימים: מנהל אופליין משוחזר רק לאחר אימות קודם של אותו חשבון. תשובה מאוחרת אינה מעניקה הרשאה ואינה מעדכנת מטמון מדיניות לאחר ביטול.
- רענון רקע של זהות שכבר הוכרעה אינו מחזיר את כל המסך למצב טעינה.
- בקשות קריאה לענן מוגבלות ל־20 שניות; הגדרות תצוגה ורענון מדיניות תוכן ל־8 שניות. אין ניסיון חוזר אוטומטי לפעולות כתיבה ואין הצגת הצלחה מזויפת.
- מטמון הגדרות שמור נשמר כאשר הרשת נכשלת. כישלון או רשימת תפקידים ריקה אינם משאירים את חישוב התצוגה במצב טעינה נצחי.
- הפרדה בין טעינת החשבון, טעינת נתוני הלימוד והכנת התצוגה. לאחר 15 שניות מוצעת טעינה חוזרת ללא מחיקת הנתונים.

## בדיקות

- `npm test -- --reporter=dot`: כל 345 הבדיקות עברו, 67 קבצים, 0 כישלונות, 13.33 שניות.
- נבדקו בקשה שאינה מסתיימת ואינה מכבדת ביטול, תוצאה מאוחרת, מעבר זהות, מנהל מאומת ולא מאומת, פרופיל חסום, רשימת תפקידים ריקה, רענון רקע ושחזור מדיניות שמורה.
- בדיקות TypeScript ומניעת כפילויות עברו.
- בניית Web ובניית Android עברו.
- בדיקות העורך החי: 2 עברו, 0 נכשלו, במידות 390×900 ו־1440×900. כתובת הבדיקה הזמנית הייתה `http://127.0.0.1:65287`; השרת נסגר בסיום. העורך האמיתי נבדק עם מתאמי זהות וענן מבודדים.

## התקנה

- הותקן עדכון באמצעות `adb install --user 0 -r` במכשיר `RZGL41JMEVX`.
- חבילה: `com.pashash.app.debug`, התקנת פיתוח 25, גרסת Android `2.7.31-dev.25`, קוד Web `2.7.33`.
- זמן העדכון המאומת במכשיר: 18.09.2026 11:07:56.
- האפליקציה והנתונים לא נמחקו. גם הטוגל הצף של העריכה החיה נכלל בחבילה.
- לאחר פתיחת הגרסה המותקנת נצפה מסך הבית עם הרשאת מנהל וללא הודעת חסימה או טעינה. צילום: `.codex-runtime/phone-permissions-after-dev25.png`.
- תצפית חיה של 75 שניות, כולל מעבר מחזור הרענון התקופתי: 25 מתוך 25 דגימות הציגו מנהל, טעינת חשבון והרשאות כבויה, ללא חסימה וללא חזרה למסך ההמתנה. נתונים: `.codex-runtime/phone-startup-observation.json`. הדגימה התחילה לאחר פתיחת האפליקציה ולכן אינה מדידת זמן האתחול המדויק.

## גבולות האימות

בדיקות ההשהיה והכשל בוצעו בבדיקות אוטומטיות מבוקרות. בדיקת המכשיר החי אינה הוכחה לכל תרחיש השהיה של Android, לכל רשת או לשימוש אופליין ממושך. לא בוצעו שינויים בהרשאות בענן, לא פורסם עדכון לחנויות ולא נבנה מתקין Windows במסגרת תיקון זה.
