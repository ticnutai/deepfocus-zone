# גרסת Windows ‏2.7.11

נבנתה ופורסמה ב־8 בספטמבר 2026.

## קבצים

- מתקין: `release/lemaan-setup-2.7.11.exe`
- גודל: 130,208,467 בתים
- SHA-256: `8E926EFA78B36ACF97E92A4051F66E4CFC0476B4F994F93EA60C7FC419D0AD54`
- קובץ עדכון דיפרנציאלי: `release/lemaan-setup-2.7.11.exe.blockmap` — 136,220 בתים
- מטא־דאטה לעדכון אוטומטי: `release/latest.yml` — גרסה 2.7.11

GitHub פרסם שלושה נכסים והגדיר את v2.7.11 כגרסה האחרונה. כתובת ההורדה הציבורית החזירה HTTP 200 וגודל זהה למתקין המקומי. ה־digest שדווח ב־GitHub זהה ל־SHA-256 המקומי, וקובץ `latest.yml` המרוחק זהה לקובץ המקומי.

## בדיקות לאחר הבנייה

- בניית Vite ל־Electron: הצליחה, 3,714 מודולים.
- יצירת NSIS ל־Windows x64: הצליחה.
- 178 מתוך 178 בדיקות אוטומטיות עברו.
- בדיקת TypeScript עברה ללא שגיאות.
- קובצי ה־production הוגשו בשרת בדיקה מבודד ב־`http://127.0.0.1:5001` והחזירו HTTP 200.
- 6 מתוך 6 תרחישי הרשאות וממשק עברו מול קובצי ה־production.
- כותרת PE תקינה (`MZ`), וגרסת הקובץ וגרסת המוצר הן 2.7.11.
- מנגנון `electron-updater` מוגדר ל־`ticnutai/deepfocus-zone`, עם הורדה אוטומטית והתקנה ביציאה.

## מגבלות

המתקין אינו חתום בחתימת Authenticode ולכן Windows/SmartScreen עשוי להציג אזהרת מפרסם לא מוכר. לא בוצעה התקנה על המחשב ולא נבדק מעבר בפועל מגרסה מותקנת ישנה ל־2.7.11; נבדקו קובצי העדכון, הכתובות וההתאמה הקריפטוגרפית.

השינויים המקומיים וקובצי הגרסה לא בוצעו להם commit או push לענף המקור במסגרת בנייה זו. קובץ ההתקנה שפורסם כן נבנה מהמצב המקומי הנוכחי.
