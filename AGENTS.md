# כללי עבודה קבועים לפרויקט

## מקור אמת יחיד ומניעת כפילויות

לפני הוספת מסך, טאב, טבלה, RPC, מפתח אחסון, מנגנון הרשאות או תהליך סנכרון:

1. מחפשים תחילה את המימוש הקיים בקוד, במיגרציות ובבדיקות.
2. מרחיבים את מקור האמת הקיים; לא יוצרים מנגנון מקביל לאותה פעולה.
3. אם מחליפים מנגנון ישן, משאירים קורא תאימות רק לצורך מעבר ומסירים כותב כפול.
4. מריצים `npm run qa:no-duplicates` ואת הבדיקות הממוקדות של התחום.
5. שינוי לא נחשב שלם אם שתי זרימות כותבות את אותו מידע באופן שונה או אם שני טאבים מבצעים אותה פעולה.

בסיווג שאלות המקור היחיד הוא רשומת `cards`: השדות `masechta`, `daf`, `amud` יחד עם שרשרת תגיות הקטגוריה הקיימת. אישור מנהל משתמש ב־`publish_user_question`; אין ליצור טבלת סיווג או תהליך פרסום נוסף.

בזהות משתמש אין להציג UUID, מפתח מכשיר, כתובת `@users.local` או מזהה `offline-*`. במסכי ניהול מציגים רק שם משתמש/שם תצוגה ומייל אמיתי, באמצעות `publicUserIdentity`.
